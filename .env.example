# Opinator Configuration - Unified variables for devbox and docker-compose

# Environment Configuration
# Options: "local" (uses Docker PostgreSQL), "production" (uses Supabase)
FASTAPI_ENV=local

# Service URLs (will change automatically depending on environment)
HEADLESSX_URL=http://localhost:3001

# PostgreSQL Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5435
POSTGRES_DB=opinator
POSTGRES_USER=opinator
POSTGRES_PASSWORD=opinator123

# Additional Configuration
MAX_REVIEWS_PER_PLATFORM=25
CACHE_TTL_SECONDS=3600
DEBUG=true

# HeadlessX Authentication (generate with: openssl rand -hex 32)
AUTH_TOKEN=your_secure_random_token_here

# Google Places API Configuration
# Get your API key from: https://console.cloud.google.com/
# Enable Places API for your project
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# Supabase Configuration (for production environment)
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here

